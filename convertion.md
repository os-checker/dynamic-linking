# 同步函数转换成异步函数

这涉及几个层面的考量：
* 在语法层面，异步函数本身可以调用同步函数，因此任何同步函数都可以放置在异步函数内，例如

```rust
fn sleep_(ms: u64) {}
async fn sleep(ms: u64) { sleep_(ms) }
```

* 在执行层面，同步函数在调用过程中必须一直执行直至函数返回；异步函数在非 await 点也是一路执行，但遇到
  await 点会让渡执行权给异步运行时，运行时通过轮询 Future 状态机来决定切换任务执行或者交还执行权，当让渡点的状态就绪，异步函数才在
  await 点之后执行。

* 在必要性上，同步函数是否值得改为异步？
  * 异步的高效之处在于节省事件的等待时间，因此只有函数的某些操作需要花一些等待事件发生，那么才值得实现为异步，并且需要针对这些事件精心设计异步运行时。
  * 异步函数的优化问题：编译器对同步函数具有成熟的优化实现，但对异步函数则不一定，比如上面的 sleep
    函数没有任何 await 点，但在汇编级别可能存在状态机的代码，但实际可以直接内联成同步函数 sleep_ 以减少指令和逻辑。
